spring:
  application:
    name: auth-server
  datasource:
    url: jdbc:mysql://localhost:3306/webmapia_auth
    username: scott
    password: tiger

mybatis:
  type-aliases-package: nsl.webmapia.authserver.domain.**.entity
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: mapper/**/*.xml

logging:
  level:
    nsl:
      webmapia:
        authserver: trace
    org:
      springframework:
        security: trace


client:
  origin: http://localhost:3000